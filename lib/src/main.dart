import 'package:terbilang/src/terbilang.dart';

void main() {
  Terbilang _terbilang = Terbilang(number: 999);
  print(_terbilang.result());
  // print(_terbilang.make(number: 999));
  // print(_terbilang.make(number: 999));
}
